/**
 * Advanced Gallery Block — Frontend Script
 *
 * Lightweight initialization for gallery enhancements.
 * Lazy loading is handled natively via loading="lazy" + decoding="async".
 * Masonry layout is handled entirely by CSS columns.
 * No jQuery dependency.
 */
(function () {
    'use strict';

    // Reserved for future interactive enhancements.
    // Lazy loading and responsive images (srcset/sizes) are now handled
    // natively by the browser and wp_get_attachment_image().

})();
